# Private workspace map (example - do not commit real inventory)

Copy to `workspace.private.md` on each machine and fill in. That file is gitignored.

This section is **appended** to machine law at install time.

## Private product inventory

| Path | Role |
|------|------|
| `~/code/personal/example-app` | Example personal product |
| `~/code/work/example-work-repo` | Example work monorepo |

## Dead paths (optional)

List retired paths so agents do not invent them:

- `~/code/personal/old-name`

## Notes

- Keep client names, finance systems, and internal work codenames **only** in `workspace.private.md`.
- Do not put API keys, tokens, emails, or account IDs here either - use 1Password / local env loaders.
