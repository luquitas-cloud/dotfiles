# Agent Instructions - `~/code`

This directory is a **container**, not a product.

## Layout

| Path | Role |
|------|------|
| `personal/` | Personal products and experiments |
| `work/` | Work products |

Concrete product names live in the machine private map (`dotfiles/agents/law/workspace.private.md`), not in this public template.

## Rule

For real coding work, **change into (or open) the specific repository root** so that repo `AGENTS.md` loads. Do not treat this folder as a substitute for product law.

Machine-wide defaults: portable pack `~/dotfiles/agents/law/AGENTS.md` (installed into tool homes).
